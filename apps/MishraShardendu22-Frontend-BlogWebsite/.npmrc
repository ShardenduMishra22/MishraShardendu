# pnpm configuration
# Suppress deprecation warnings
# These warnings come from drizzle-kit's subdependencies (@esbuild-kit packages)
# which we cannot directly control. They will be resolved when drizzle-kit updates.
audit=false
fund=false
